# Local environment and secrets
.env
.env.*
!.env.example
secrets/*
!secrets/.gitkeep
!secrets/README.md

# Runtime state mounted into the container
data/
workspace/
scheduler/logs/

# Local-only agent/workspace instructions
AGENTS.md

# Build, editor, and OS noise
.venv/
node_modules/
.DS_Store
*.log

# Git metadata should never be nested into this repo
.git/
