# Todo

## Core
- Make LLM auth easy
- Make it easy to manage extensions

# Extensions
- Make it easy to auth into gog
- Add slack support

# Permissions
- Make it easy to add volumes (safety first)
- Make it easy to add host commands (safety first)
- Create whitelist commands support (host and local)
- Create blacklist commands support (host and local)
- Create HITL container:  LLM command -> Filtered by allowlist -> HITL -> Human -> (bash OR trash)

